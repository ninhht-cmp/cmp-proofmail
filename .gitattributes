# Windows batch files MUST use CRLF or cmd.exe misparses them (window flashes/
# closes). Shell launchers MUST stay LF. Pin endings so they're correct after
# clone and checkout on any OS.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.sh  text eol=lf
*.command text eol=lf
